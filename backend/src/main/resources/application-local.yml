spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/fitnessapp
    username: postgres
    password: postgres

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/fitnessapp
    user: postgres
    password: postgres

firebase:
  credentials-path: firebase-credentials.json
  project-id: fitness-app-local

logging:
  level:
    com.fitnessapp: DEBUG
    org.springframework.r2dbc: DEBUG
